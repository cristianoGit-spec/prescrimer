# Railway Environment Variables Template
# Configure estas variáveis no Railway Dashboard

# Port (Railway configura automaticamente)
PORT=${{PORT}}

# MongoDB Configuration
# OPÇÃO 1: MongoDB Atlas (recomendado para produção)
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/prescrimed?retryWrites=true&w=majority

# OPÇÃO 2: MongoDB Railway Plugin
# MONGODB_URI=${{MONGO_URL}}

# OPÇÃO 3: Usar MongoDB Memory Server (temporário - apenas para testes)
# USE_MEMORY_DB=true

# JWT Secret (obrigatório - gerar com: openssl rand -base64 32)
JWT_SECRET=${{JWT_SECRET}}

# Node Environment
NODE_ENV=production
NODE_ENV=production

# CORS Origins (opcional - por padrão permite todos)
# CORS_ORIGIN=https://prescrimed.netlify.app

# Log Level (opcional)
# LOG_LEVEL=info
